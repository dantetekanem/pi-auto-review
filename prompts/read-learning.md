Read what earlier reviews consolidated about a codebase. Only the top-level review session calls `agentic_code_review_read_learning`, at intake, with the reviewed checkout's absolute `root` and its stable `repository` identity.

It canonicalizes HTTPS, bare-host, git and owner/repo spellings to one repository identity. If older runs split equivalent notes across folders, it returns the newest `current` block per topic and all history paths; the next save writes the canonical folder. It returns the `current` block of every topic (`structure`, `design`, `framework`, `testing`, `pitfalls`, `decisions`, `methods` and any other saved topic): the consolidated text the last finishing run wrote, with its run and revision, plus how many runs sit in each topic's history and where the file is. It never returns the history bodies; read a topic file by line range only when a current line needs its provenance.

Treat every current line as a claim to revalidate against the reviewed revision, not as evidence. A `decisions` line settles a question again only when the pack shows the cited symbol unchanged. Record what you reused, refuted or corrected in the map with the owning run ID, and fold the outcome into the current blocks you save at the end of this run.
