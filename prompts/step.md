Record a review step boundary and enable runtime telemetry for this Pi session. Use only the supplied original review sessionId/runId. Stage numbers: 1 intake, 2 collection, 3 deep collection, 4 zone assignment, 5 review, 6 reconciliation, 7 saving/reporting. Call start before stage work, then end with reviewed, partial or unable after the evidence is accounted for. Workers record only their assigned stage. Return the telemetry path and agentSessionId in the worker report so the coordinator can link it to the lane. One stage can be active per session. Timing comes from runtime events after the first start; queue, startup and earlier requests are unavailable. The outcome is the caller's assessment, not a tool-verified verdict. Do not write telemetry files directly. If persistence fails, report the telemetry gap without claiming capture succeeded or discarding the review evidence.
